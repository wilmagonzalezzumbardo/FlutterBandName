import 'package:flutter/material.dart';

class MantoScreen extends StatelessWidget {
   
  const MantoScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('MantoScreen'),
      ),
    );
  }
}